# server
server.port=8090

# spring.datasource.type=org.apache.tomcat.jdbc.pool.DataSource
spring.datasource.driverClassName=com.mysql.jdbc.Driver
spring.datasource.url=jdbc:mysql://61.41.4.26:3306/spring_boot?useSSL=true&verifyServerCertificate=false
spring.datasource.username=springboot
spring.datasource.password=spb-1234


# JPA
# 기존에 생성되 있던 테이블들을 삭제하고 새로 만듬
# spring.jpa.hibernate.ddl-auto=create
# create 와 같은 동작을 하나 종료시에 DROP 한다
# spring.jpa.hibernate.ddl-auto=create-drop
# 변경된 부분만 방영
spring.jpa.hibernate.ddl-auto=update
# 테이블과 Entiry 가 매핑되는지 유효성 검사를 실행한다
# spring.jpa.hibernate.ddl-auto=validate
# 초기화 동작을 하지 않는다
# spring.jpa.hibernate.ddl-auto=none

spring.jpa.generate-ddl=false
spring.jpa.show-sql=true
spring.jpa.database=mysql
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.database-platform=org.hibernate.dialect.MariaDBDialect

# JPA naming
spring.jpa.hibernate.naming.implicit-strategy=org.hibernate.boot.model.naming.ImplicitNamingStrategyLegacyJpaImpl
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

